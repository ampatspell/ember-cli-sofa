import Store from './store';
import Model from './model';

import {
  id,
  prefix,
  rev,
  type,
  attr,
  belongsTo
} from './properties/helpers';

export {
  Store,
  Model,

  id,
  prefix,
  rev,
  type,
  attr,
  belongsTo
};

export default Store;
