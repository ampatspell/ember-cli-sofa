import Store from './store';
import Model from './model';

import {
  id,
  prefix,
  rev,
  type,
  attr,
} from './properties/helpers';

export {
  Store,
  Model,

  id,
  prefix,
  rev,
  type,
  attr
};

export default Store;
